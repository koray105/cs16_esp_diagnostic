# Local Binary Disassembly Preference Rule
- When debugging, reverse-engineering, or analyzing game/system behavior where target binaries (DLLs/EXEs) are available on disk, prioritize direct disassembling and runtime memory inspection over web searches.
- Write minimal local analysis scripts in C++ or PowerShell using local tools to extract exact struct strides, exported functions, and dispatch tables directly.
